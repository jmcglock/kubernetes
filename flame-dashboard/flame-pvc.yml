apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flame
  namespace: flame
  labels:
    app: flame
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi