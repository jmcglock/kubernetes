apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: uptime
  name: uptime
  namespace: uptime
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi